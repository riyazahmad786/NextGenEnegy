import { RemoveNegativeSignPipe } from './remove-negative-sign.pipe';

describe('RemoveNegativeSignPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveNegativeSignPipe();
    expect(pipe).toBeTruthy();
  });
});
